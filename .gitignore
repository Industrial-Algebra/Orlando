/target
/pkg
/pkg-temp
/pkg-test
node_modules
*.log
Cargo.lock
.DS_Store
# proptest regression seeds (default `*.proptest-regressions` dirs + custom `proptest-regressions/`)
*.proptest-regressions
proptest-regressions/
# agent/MCP config (machine-specific absolute paths)
.mcp.json
book/book/
